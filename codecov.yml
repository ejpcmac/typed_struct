# SPDX-FileCopyrightText: NONE
# SPDX-License-Identifier: CC0-1.0

codecov:
  branch: develop

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 100%

comment:
  layout: "diff, files, components"
